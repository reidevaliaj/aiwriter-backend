# Nginx configuration update for AIWriter backend
# Add this to your nginx site configuration

# Increase proxy timeouts for AI article generation
proxy_connect_timeout 300s;
proxy_send_timeout 300s;
proxy_read_timeout 300s;

# Increase client timeouts
client_body_timeout 300s;
client_header_timeout 300s;

# Increase keepalive timeout
keepalive_timeout 300s;

# Example full location block:
# location / {
#     proxy_pass http://127.0.0.1:8080;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     
#     # Timeout settings
#     proxy_connect_timeout 300s;
#     proxy_send_timeout 300s;
#     proxy_read_timeout 300s;
# }
