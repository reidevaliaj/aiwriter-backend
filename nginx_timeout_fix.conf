# Nginx timeout configuration for AIWriter
# Add these lines to your Nginx server block

# Increase proxy timeouts for AI requests
proxy_connect_timeout 300s;
proxy_send_timeout 300s;
proxy_read_timeout 300s;

# Increase client timeouts
client_body_timeout 300s;
client_header_timeout 300s;

# Increase keepalive timeout
keepalive_timeout 300s;

# Example server block:
# server {
#     listen 80 default_server;
#     server_name _;
#     
#     # Add the timeout settings above here
#     
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }